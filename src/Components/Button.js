import React from "react";
import "../css/NavigationBar.css";

const Button = ({value}) =>{
    return(
    <div id="button">

            <button>{value}</button>
            </div>

    )}
export default Button;